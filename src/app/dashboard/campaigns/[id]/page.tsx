// =============================================================================
// [임시 비활성화] 캠페인 상세 페이지
// 나중에 사용할 수 있으므로 주석 처리하여 보관합니다.
// =============================================================================

import { redirect } from 'next/navigation'

export default async function CampaignDetailPage({ params }: { params: { id: string } }) {
  // 임시로 대시보드로 리다이렉트
  redirect('/dashboard')
}

// =============================================================================
// 원본 코드 (나중에 복원 시 사용)
// 복원 시 git history에서 확인: git show HEAD~1:src/app/dashboard/campaigns/[id]/page.tsx
// =============================================================================
